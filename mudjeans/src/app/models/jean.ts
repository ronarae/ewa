export class jean {
  productCode: String;
  description: String;
  size: String;

  constructor(productCode, description, size) {
    this.productCode = productCode;
    this.description = description;
    this.size = size;
  }
}
